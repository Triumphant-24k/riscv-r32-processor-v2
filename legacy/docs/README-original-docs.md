# Legacy material

The original subset implementation is preserved in `legacy/rtl/`, its old tests
in `legacy/tb/`, and historical images in `legacy/docs/`. They are comparison
material, not evidence for the maintained core in `rtl/`.
