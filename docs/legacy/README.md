# Legacy material

The original subset implementation remains in `src/`, its waveform-oriented
testbenches remain directly under `tb/`, and historical screenshots remain in
`docs/`. They are preserved for comparison. The maintained RV32I implementation
is in `rtl/`; new self-checking tests are under `tb/unit/` and `tb/core/`.
